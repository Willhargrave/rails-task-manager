<div class="container mx-auto">
<div class="card" style="width: 18rem;">
 <h2 class="card-title"><%= @task.title %></h2>
<div class ="card-body">
  <h3><%= @task.details %></h3>
  <%= link_to "Back",  tasks_path, class: "btn btn-primary"%>
<%= link_to "Edit", edit_task_path, class: "btn btn-danger"%>
  <%= link_to "Done", task_path(@task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "btn btn-success"%>
  </div>
</div>
</div>
